# JLPT N2 学习网站 - 产品需求文档 (PRD)
## Final Version - 面向 Claude Code

---

## 📋 文档信息

**项目名称**: JLPT N2 Learning Platform  
**目标用户**: N3合格者，准备系统学习N2  
**技术方案**: 纯前端 + IndexedDB  
**预计开发周期**: 2-3周 MVP，4周完整版  
**数据来源**: https://github.com/mxggle/anki-jlpt-n2-grammar-example-sentences

---

## 1. 项目概述

### 1.1 产品目标
开发一个**零成本、开箱即用**的JLPT N2学习网站，帮助N3合格者按照科学的学习路径完成N2语法的系统学习。

### 1.2 目标用户画像
- **基础水平**: 已通过N3考试，掌握基本日语语法
- **学习目标**: 3-4个月内系统掌握N2语法
- **学习习惯**: 希望有清晰的学习路径，每天学习15-30分钟
- **技术背景**: 普通用户，不需要安装任何软件

### 1.3 核心价值主张
✅ **循序渐进**: 50课结构化学习路径，顺序解锁  
✅ **科学复习**: 艾宾浩斯遗忘曲线，自动提醒复习  
✅ **即时反馈**: 课后测试，错题本，学习统计  
✅ **零门槛**: 打开浏览器就能用，无需注册登录  
✅ **离线可用**: PWA支持，地铁上也能学  

---

## 2. 数据源分析

### 2.1 源数据结构
**来源**: `shin-kanzen-n2-grammar/notes.csv`

**CSV字段**:
| 字段名 | 说明 | 示例 |
|--------|------|------|
| 例句 | 日语原文 | 彼にとって、家族は何よりも大切だ |
| 语法点 | 语法点 | ～にとって |
| 课号 | 课程编号 | 2 |
| 假名标注 | 完整假名 | かれにとって、かぞくはなによりもたいせつだ |
| 中文翻译 | 中文翻译 | 对他来说，家人比什么都重要 |
| 音频 | 文件名 | [sound:example_002.mp3] |
| 语法接续 | 接续方式 | 名词 + にとって |
| 语法解释 | 详细说明 | 表示"对...来说"，表示立场或角度 |
| 逐词精解 | 分词解释 | 彼(かれ):他;に:向;とって:对于... |
| 标签 | 分类标签 | N2, 语法点 |

**音频文件**: 位于 `medias/` 目录

### 2.2 数据处理需求
1. 解析CSV，提取所有字段
2. 处理音频路径（去除`[sound:]`标记）
3. 按课号分组（1-50课）
4. 按语法点分组（约200个语法点）
5. 计算每课的语法点数量和例句数量

---

## 3. 技术架构

### 3.1 技术栈

**前端框架**
```
React 18 + TypeScript
Vite (构建工具)
React Router 6 (路由)
Tailwind CSS (样式)
```

**状态管理**
```
Zustand (轻量级状态管理)
```

**数据存储**
```
IndexedDB (通过 Dexie.js)
```

**数据处理**
```
Papa Parse (CSV解析)
```

**UI组件**
```
Lucide React (图标)
自定义组件 (无需组件库)
```

**音频处理**
```
HTML5 Audio API
```

**部署**
```
Vercel / Netlify (静态网站托管，免费)
```

### 3.2 项目结构

```
jlpt-n2-learning/
├── public/
│   ├── data/
│   │   └── notes.csv                 # 从源项目复制
│   ├── audio/
│   │   └── *.mp3                     # 从源项目medias/复制
│   ├── manifest.json                 # PWA配置
│   └── favicon.ico
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx            # 顶部导航栏
│   │   │   ├── Sidebar.tsx           # 侧边栏（可选）
│   │   │   └── Footer.tsx            # 底部版权信息
│   │   ├── study/
│   │   │   ├── LessonMap.tsx         # 课程地图（关卡式）
│   │   │   ├── LessonCard.tsx        # 单个课程卡片
│   │   │   ├── GrammarIntro.tsx      # 语法点介绍页
│   │   │   ├── StudyCard.tsx         # 学习卡片（例句）
│   │   │   └── AudioPlayer.tsx       # 音频播放器
│   │   ├── practice/
│   │   │   ├── FillBlankQuiz.tsx     # 填空题
│   │   │   ├── MultipleChoiceQuiz.tsx # 选择题
│   │   │   ├── QuizResult.tsx        # 测试结果页
│   │   │   └── QuestionCard.tsx      # 题目卡片
│   │   ├── progress/
│   │   │   ├── ProgressDashboard.tsx # 进度仪表盘
│   │   │   ├── DailyGoal.tsx         # 每日目标
│   │   │   ├── WeeklyChart.tsx       # 本周学习图表
│   │   │   ├── ReviewReminder.tsx    # 复习提醒
│   │   │   ├── AchievementBadge.tsx  # 成就徽章
│   │   │   └── StudyStreak.tsx       # 连续学习天数
│   │   └── common/
│   │       ├── Button.tsx            # 按钮组件
│   │       ├── ProgressBar.tsx       # 进度条
│   │       ├── Modal.tsx             # 模态框
│   │       ├── LoadingSpinner.tsx    # 加载动画
│   │       └── EmptyState.tsx        # 空状态提示
│   ├── pages/
│   │   ├── HomePage.tsx              # 首页（学习建议+快速开始）
│   │   ├── OnboardingPage.tsx        # 首次使用引导
│   │   ├── LessonListPage.tsx        # 课程列表（关卡地图）
│   │   ├── LessonDetailPage.tsx      # 课程详情
│   │   ├── StudyPage.tsx             # 学习页面
│   │   ├── GrammarDetailPage.tsx     # 语法点详情
│   │   ├── PracticePage.tsx          # 练习模式选择
│   │   ├── QuizPage.tsx              # 测试页面
│   │   ├── ReviewPage.tsx            # 复习页面
│   │   ├── ProgressPage.tsx          # 进度中心
│   │   ├── WrongAnswersPage.tsx      # 错题本
│   │   ├── AchievementsPage.tsx      # 成就页面
│   │   └── SettingsPage.tsx          # 设置页面
│   ├── hooks/
│   │   ├── useAudio.ts               # 音频播放Hook
│   │   ├── useProgress.ts            # 进度管理Hook
│   │   ├── useReviewSchedule.ts      # 复习计划Hook
│   │   ├── useStudySession.ts        # 学习会话Hook
│   │   ├── useQuiz.ts                # 测试Hook
│   │   └── useIndexedDB.ts           # IndexedDB操作Hook
│   ├── stores/
│   │   ├── userStore.ts              # 用户状态（进度、设置）
│   │   ├── studyStore.ts             # 学习状态（当前课程、例句）
│   │   ├── quizStore.ts              # 测试状态
│   │   └── settingsStore.ts          # 用户设置
│   ├── utils/
│   │   ├── csvParser.ts              # CSV解析工具
│   │   ├── reviewAlgorithm.ts        # 艾宾浩斯复习算法
│   │   ├── quizGenerator.ts          # 测试题生成器
│   │   ├── dataProcessor.ts          # 数据处理（分组、排序）
│   │   ├── dateHelper.ts             # 日期处理工具
│   │   └── constants.ts              # 常量定义
│   ├── types/
│   │   ├── sentence.ts               # 例句类型定义
│   │   ├── lesson.ts                 # 课程类型定义
│   │   ├── grammar.ts                # 语法点类型定义
│   │   ├── progress.ts               # 进度类型定义
│   │   ├── quiz.ts                   # 测试类型定义
│   │   └── achievement.ts            # 成就类型定义
│   ├── db/
│   │   ├── schema.ts                 # Dexie数据库Schema
│   │   ├── migrations.ts             # 数据库迁移
│   │   └── operations.ts             # 数据库操作封装
│   ├── App.tsx                       # 根组件
│   ├── main.tsx                      # 入口文件
│   └── router.tsx                    # 路由配置
├── .env.example                      # 环境变量示例
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
└── README.md
```

---

## 4. 数据模型设计

### 4.1 IndexedDB 数据库设计

**数据库名称**: `JLPTN2DB`  
**版本**: 1

#### 表1: `sentences` (例句表)
```typescript
interface Sentence {
  id: string;                    // 唯一ID（自动生成）
  lessonNumber: number;          // 课号 (1-50)
  grammarPoint: string;          // 语法点名称
  sentence: string;              // 日语原文
  furigana: string;              // 假名标注
  translation: string;           // 中文翻译
  audioPath: string;             // 音频文件路径
  grammarConnection: string;     // 语法接续
  grammarExplanation: string;    // 语法解释
  wordByWord: string;            // 逐词精解
  tags: string[];                // 标签数组
}

// 索引
indexes: ['lessonNumber', 'grammarPoint']
```

#### 表2: `lessons` (课程表，计算生成)
```typescript
interface Lesson {
  id: number;                    // 课号 (1-50)
  grammarPoints: string[];       // 该课包含的语法点列表
  sentenceCount: number;         // 例句总数
  isUnlocked: boolean;           // 是否解锁
  isCompleted: boolean;          // 是否完成
  completionRate: number;        // 完成度 (0-100)
}

// 主键: id
```

#### 表3: `grammarPoints` (语法点表，计算生成)
```typescript
interface GrammarPoint {
  id: string;                    // 语法点名称（唯一）
  lessonNumber: number;          // 所属课号
  sentenceIds: string[];         // 包含的例句ID列表
  sentenceCount: number;         // 例句数量
  grammarConnection: string;     // 接续方式
  grammarExplanation: string;    // 详细说明
  isLearned: boolean;            // 是否已学习
}

// 主键: id
// 索引: lessonNumber
```

#### 表4: `userProgress` (用户进度)
```typescript
interface UserProgress {
  id: string;                    // 'user_progress' (单用户固定值)
  currentLessonId: number;       // 当前学习的课程ID
  currentGrammarPoint: string;   // 当前学习的语法点
  learnedSentences: string[];    // 已学习的例句ID列表
  learnedGrammar: {
    grammarId: string;           // 语法点ID
    firstLearnedDate: Date;      // 首次学习日期
    lastReviewedDate: Date;      // 最后复习日期
    nextReviewDate: Date;        // 下次复习日期
    reviewCount: number;         // 复习次数
    masteryLevel: number;        // 掌握程度 (0-5)
  }[];
  completedLessons: number[];    // 已完成的课程ID列表
  totalStudyTime: number;        // 总学习时长（秒）
  studyStreak: number;           // 连续学习天数
  lastStudyDate: Date;           // 最后学习日期
}

// 主键: id
```

#### 表5: `dailyGoals` (每日目标)
```typescript
interface DailyGoal {
  id: string;                    // 日期字符串 'YYYY-MM-DD'
  date: Date;                    // 日期
  targetSentences: number;       // 目标例句数（默认10）
  targetGrammarPoints: number;   // 目标语法点数（默认2）
  completedSentences: number;    // 已完成例句数
  completedGrammarPoints: number;// 已完成语法点数
  studyTime: number;             // 学习时长（秒）
  isCompleted: boolean;          // 是否完成
}

// 主键: id
// 索引: date
```

#### 表6: `exerciseHistory` (练习历史)
```typescript
interface ExerciseRecord {
  id: string;                    // 自动生成UUID
  sentenceId: string;            // 例句ID
  grammarPoint: string;          // 语法点
  questionType: 'fill' | 'choice'; // 题型
  userAnswer: string;            // 用户答案
  correctAnswer: string;         // 正确答案
  isCorrect: boolean;            // 是否正确
  timestamp: Date;               // 答题时间
}

// 主键: id
// 索引: ['sentenceId', 'timestamp', 'isCorrect']
```

#### 表7: `wrongAnswers` (错题本)
```typescript
interface WrongAnswer {
  id: string;                    // sentenceId
  sentenceId: string;            // 例句ID
  grammarPoint: string;          // 语法点
  wrongCount: number;            // 答错次数
  lastWrongDate: Date;           // 最后答错日期
  resolved: boolean;             // 是否已掌握（连续答对3次）
  correctStreak: number;         // 连续答对次数
}

// 主键: id
// 索引: ['resolved', 'grammarPoint']
```

#### 表8: `achievements` (成就记录)
```typescript
interface Achievement {
  id: string;                    // 成就ID
  name: string;                  // 成就名称
  description: string;           // 成就描述
  icon: string;                  // 图标（emoji）
  condition: string;             // 解锁条件
  isUnlocked: boolean;           // 是否解锁
  unlockedDate?: Date;           // 解锁日期
}

// 主键: id
```

---

## 5. 核心功能详细设计

### 5.1 学习路径系统

#### 5.1.1 课程大纲页面 (LessonListPage)

**视觉设计**: 游戏关卡地图风格

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         N2 学习之路
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    ┌─────────┐
    │ 课程 1  │ ✓ 已完成
    │ 4语法点 │
    │ 20例句  │
    └─────────┘
         │
    ┌─────────┐
    │ 课程 2  │ ⏳ 学习中 (60%)
    │ 3语法点 │
    │ 15例句  │
    └─────────┘
         │
    ┌─────────┐
    │ 课程 3  │ 🔒 未解锁
    │ 5语法点 │
    │ 18例句  │
    └─────────┘
         │
        ...
```

**功能需求**:
1. 显示全部50课
2. 课程状态:
   - ✓ 已完成（绿色）
   - ⏳ 学习中（蓝色，显示完成百分比）
   - 🔒 未解锁（灰色，不可点击）
3. 顺序解锁: 必须完成课程N才能开始课程N+1
4. 点击已解锁课程进入课程详情页
5. 显示总体进度: "已完成 2/50 课程 (4%)"

**交互**:
- 鼠标悬停课程卡片显示详细信息弹窗
- 点击锁定课程提示"请先完成前一课"
- 支持键盘方向键导航

#### 5.1.2 课程详情页 (LessonDetailPage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
课程 2: 立场与观点的表达
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

进度: ████████░░ 80% (12/15例句)

📚 本课语法点:
─────────────────────────────
1. ～にとって
   接续: 名词 + にとって
   状态: ✓ 已完成 (5/5例句)
   
2. ～において
   接续: 名词 + において
   状态: ⏳ 学习中 (4/6例句)
   
3. ～に対して
   接续: 名词 + に対して
   状态: 📝 未开始 (0/4例句)

[继续学习] [复习本课] [课后测试]

预计剩余时间: 15分钟
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. 显示课程标题（如果有）
2. 显示完成度进度条
3. 列出本课所有语法点，每个显示:
   - 语法点名称
   - 接续方式（简短）
   - 学习状态和进度
4. 操作按钮:
   - **继续学习**: 从第一个未完成的语法点开始
   - **复习本课**: 重新浏览所有例句（快速模式）
   - **课后测试**: 完成80%后解锁，测试本课知识点
5. 显示预计剩余学习时间

#### 5.1.3 语法点介绍页 (GrammarIntro)

在开始学习每个新语法点时显示:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
课程 2 > 语法点 1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

～にとって

━━━ 接续方式 ━━━
名词 + にとって

━━━ 含义 ━━━
表示"对...来说"，强调站在某个立场
或角度看待事物。

━━━ 例子 ━━━
• 学生にとって
  对学生来说
  
• 会社にとって
  对公司来说

━━━ 注意事项 ━━━
不能用于第一人称的主观感受。

[开始学习例句] (共5个)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. 显示语法点名称
2. 显示接续方式（清晰、大字号）
3. 显示详细说明（来自CSV的语法解释字段）
4. 简单例子（如果需要，可以从例句中提取）
5. 注意事项（可选）
6. 显示该语法点的例句数量
7. "开始学习例句"按钮

#### 5.1.4 学习卡片 (StudyCard)

核心学习组件，用于显示例句:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
课程 2 > ～にとって > 例句 3/5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

彼[にとって]、家族は何よりも大切だ。
  (语法点高亮显示)

[🔊 播放音频]

─────────────────────────────
[显示假名]
─────────────────────────────
かれにとって、かぞくはなによりも
たいせつだ。
─────────────────────────────

─────────────────────────────
[显示翻译]
─────────────────────────────
对他来说，家人比什么都重要。
─────────────────────────────

─────────────────────────────
[显示解析]
─────────────────────────────
彼(かれ): 他
にとって: 对...来说
家族(かぞく): 家人
何よりも(なによりも): 比什么都
大切(たいせつ): 重要
─────────────────────────────

[⭐ 我已理解] [→ 下一句]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. **语法点高亮**: 在例句中用不同颜色/背景高亮语法点
2. **音频播放**: 
   - 点击播放按钮播放音频
   - 支持快捷键（空格键）
   - 显示播放状态（播放中/暂停）
3. **可折叠内容**:
   - 假名标注（默认隐藏，点击显示）
   - 中文翻译（默认隐藏，点击显示）
   - 逐词解析（默认隐藏，点击显示）
4. **导航**:
   - "我已理解"按钮: 标记为已学习，跳到下一句
   - "下一句"按钮: 不标记，直接下一句
   - 支持键盘方向键（←上一句，→下一句）
5. **进度指示**: 显示当前例句位置（3/5）
6. **收藏功能**: 点击⭐收藏例句，方便复习

**状态管理**:
- 点击"我已理解"时:
  1. 记录该例句为已学习（IndexedDB: userProgress.learnedSentences）
  2. 更新语法点的学习进度
  3. 如果完成该语法点的所有例句，触发完成提示

#### 5.1.5 语法点完成提示

完成一个语法点的所有例句后显示:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      🎉 完成语法点！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

～にとって

你已经学习了该语法点的全部 5 个例句！

[小测验 (3题)] [跳过，继续下一个]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
- 显示祝贺信息
- 提供课后小测（3-5题）
- 允许跳过测试，继续学习

---

### 5.2 练习系统

#### 5.2.1 课后小测 (Mini Quiz)

**触发时机**: 完成一个语法点的所有例句后

**题型**: 填空题为主

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
课后小测: ～にとって
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

题目 1/3

请选择正确的语法点填入空白处:

彼______、家族は何よりも大切だ。

A. にとって
B. において
C. に対して
D. について

[提交答案]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**提交后反馈**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ 正确！

完整句子:
彼にとって、家族は何よりも大切だ。

翻译:
对他来说，家人比什么都重要。

解析:
「にとって」表示"对...来说"，用于表达
某人的立场或角度。

[下一题]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

或者答错时:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✗ 不正确

你的答案: において
正确答案: にとって

解析:
「において」表示"在...方面/场合"，
多用于书面语，而此句表达个人立场，
应该用「にとって」。

[下一题]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**测试结果**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      测试完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

～にとって

正确: 2/3 (67%)

✓ 题目1: 正确
✗ 题目2: 错误
✓ 题目3: 正确

错误题目已加入错题本。

[继续学习下一个语法点]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. 从该语法点的例句中随机抽取3个生成题目
2. 提供4个选项（正确语法点 + 3个相似语法点）
3. 实时判断对错
4. 显示详细解析
5. 错题自动加入错题本
6. 不强制通过，但会记录分数

#### 5.2.2 课程测试 (Lesson Test)

**触发时机**: 完成课程80%以上内容后解锁

**规模**: 10-15题，涵盖本课所有语法点

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
课程 2 综合测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

共 12 题
限时 10 分钟

[开始测试]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**测试结果**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      课程 2 测试结果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

得分: 10/12 (83%)
用时: 7分32秒

✓ 已通过！(70分以上)

按语法点统计:
• ～にとって: 3/3 ✓
• ～において: 2/3
• ～に対して: 3/3 ✓
• ～について: 2/3

[解锁课程 3] [复习错题]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. 混合本课所有语法点出题
2. 倒计时功能
3. 可以暂停（保存进度）
4. 答题后统计各语法点掌握情况
5. 通过标准: 70%以上
6. 未通过可以重考（无限次）
7. 通过后解锁下一课

#### 5.2.3 练习模式页面 (PracticePage)

用户可以自主选择练习:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        练习模式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────┐
│  📝 填空练习              │
│  随机抽取例句，填写语法点  │
│  [开始练习]              │
└─────────────────────────┘

┌─────────────────────────┐
│  ✓ 选择题练习            │
│  选择正确的语法点        │
│  [开始练习]              │
└─────────────────────────┘

┌─────────────────────────┐
│  🔊 听力练习             │
│  听音频，填写完整句子     │
│  [开始练习]              │
└─────────────────────────┘

┌─────────────────────────┐
│  📚 错题本复习           │
│  复习你做错的题目        │
│  当前错题: 5 道          │
│  [开始复习]              │
└─────────────────────────┘

筛选练习范围:
[课程 1-5] [所有已学语法点] [开始]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能需求**:
1. 4种练习模式
2. 可以筛选练习范围（已学课程、特定语法点）
3. 每种模式有独立的练习页面
4. 记录每种模式的练习次数和正确率

---

### 5.3 复习系统

#### 5.3.1 艾宾浩斯复习算法

**复习间隔**:
```
首次学习 → 1天后 → 3天后 → 7天后 → 15天后 → 30天后
```

**掌握等级**:
```
Level 0: 未学习
Level 1: 刚学习（1天后复习）
Level 2: 初步掌握（3天后复习）
Level 3: 基本掌握（7天后复习）
Level 4: 熟练掌握（15天后复习）
Level 5: 完全掌握（30天后复习）
```

**等级提升规则**:
- 复习时答对 → 等级+1
- 复习时答错 → 等级-1（最低为1）
- 达到Level 5后，继续保持30天间隔复习

**实现逻辑**:
```typescript
function calculateNextReviewDate(
  currentLevel: number,
  wasCorrect: boolean
): { nextLevel: number; nextDate: Date } {
  let nextLevel = wasCorrect 
    ? Math.min(currentLevel + 1, 5)
    : Math.max(currentLevel - 1, 1);
  
  const intervals = [1, 1, 3, 7, 15, 30]; // 天数
  const daysToAdd = intervals[nextLevel];
  
  const nextDate = new Date();
  nextDate.setDate(nextDate.getDate() + daysToAdd);
  
  return { nextLevel, nextDate };
}
```

#### 5.3.2 复习提醒 (ReviewReminder)

**首页显示**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔔 今日需要复习
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5 个语法点到期需要复习:

• ～にとって (课程2, 学习于7天前)
• ～において (课程2, 学习于3天前)
• ～に対して (课程2, 学习于7天前)
• ～について (课程2, 学习于1天前)
• ～によって (课程3, 学习于3天前)

预计时间: 15分钟

[开始复习] [推迟到明天]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**复习页面** (ReviewPage):
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
复习: ～にとって (1/5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

上次学习: 7天前
掌握等级: Level 2 (初步掌握)

[显示例句学习卡片或测试题]

... 

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**复习模式**:
1. **快速浏览模式**: 显示例句卡片，快速过一遍
2. **测试模式**: 做3道题，根据正确率更新掌握等级

**复习完成**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      复习完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

今日复习: 5个语法点
正确率: 4/5 (80%)

已更新复习计划:
• ～にとって: Level 3，7天后复习
• ～において: Level 3，7天后复习
• ～に対して: Level 2，3天后复习
• ～について: Level 3，7天后复习
• ～によって: Level 3，7天后复习

继续加油！🔥

[返回首页]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 5.3.3 错题本 (WrongAnswersPage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        错题本
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

共 8 道错题

按语法点分类:
─────────────────────────────
～において (3题)
• 题目1: 最后答错于 2天前
• 题目2: 最后答错于 5天前
• 题目3: 最后答错于 2天前

～に対して (2题)
• 题目4: 最后答错于 1天前
• 题目5: 最后答错于 3天前

～について (3题)
...

[复习全部错题] [按语法点复习]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
1. 显示所有错题
2. 按语法点分类
3. 显示最后答错时间
4. 支持全部复习或按语法点复习
5. 连续答对3次后从错题本移除

---

### 5.4 进度与统计

#### 5.4.1 首页 (HomePage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   欢迎回来！继续你的N2学习之旅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔥 连续学习: 7 天

━━━ 今日学习建议 ━━━
建议学习: 课程 2 > ～において
预计时间: 15分钟
内容: 1个语法点，6个例句

[开始学习]

━━━ 今日目标 ━━━
████████░░ 80% (8/10 例句)
还需学习 2 个例句即可完成今日目标！

━━━ 今日需要复习 ━━━
🔔 5 个语法点需要复习
[开始复习]

━━━ 总体进度 ━━━
课程: 1/50 (2%)
语法点: 8/200 (4%)
例句: 40/1000 (4%)

[查看详细统计] [继续上次学习]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
1. 显示连续学习天数
2. 智能学习建议
3. 每日目标进度
4. 复习提醒
5. 总体进度概览
6. 快速操作按钮

#### 5.4.2 进度中心 (ProgressPage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        学习统计
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 总体进度 ━━━
已完成课程: 1/50 (2%)
已学习语法点: 8/200 (4%)
已学习例句: 40/1000 (4%)

━━━ 学习时长 ━━━
总学习时长: 5小时32分钟
平均每天: 47分钟
本周学习: 3小时15分钟

━━━ 练习统计 ━━━
总练习题数: 45题
总正确率: 82%
填空题正确率: 85%
选择题正确率: 78%

━━━ 本周学习 ━━━
图表显示每天学习的语法点数量:

周一: ████ 4个
周二: ██ 2个
周三: 0个 (未学习)
周四: ████████ 8个
周五: ████ 4个
周六: ██ 2个
周日: (今天)

━━━ 掌握程度分布 ━━━
Level 5 (完全掌握): 0个
Level 4 (熟练掌握): 2个
Level 3 (基本掌握): 3个
Level 2 (初步掌握): 3个
Level 1 (刚学习): 0个

━━━ 薄弱知识点 ━━━
正确率最低的3个语法点:
1. ～において: 60% (3/5)
2. ～について: 67% (2/3)
3. ～に対して: 75% (3/4)

[针对性练习]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
1. 多维度统计数据
2. 可视化图表（本周学习、掌握程度分布）
3. 薄弱知识点分析
4. 提供针对性练习入口

#### 5.4.3 成就系统 (AchievementsPage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        成就徽章
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

已解锁: 5/20

✓ 🎯 开始学习
   完成第1个语法点
   解锁于: 2024-01-03

✓ 📚 第一课
   完成第1课
   解锁于: 2024-01-05

✓ 🔥 连续7天
   连续学习7天
   解锁于: 2024-01-10

✓ 💯 满分测试
   课后测试获得满分
   解锁于: 2024-01-06

✓ ⏰ 早起学习
   上午8点前学习
   解锁于: 2024-01-08

🔒 ⭐ 25%进度
   完成25%课程
   条件: 完成13课

🔒 🏆 50%进度
   完成50%课程
   条件: 完成25课

...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**成就列表**:
1. 🎯 开始学习 - 完成第1个语法点
2. 📚 第一课 - 完成第1课
3. 🔥 连续7天 - 连续学习7天
4. 🔥 连续30天 - 连续学习30天
5. 💯 满分测试 - 课后测试获得满分
6. 🎓 完成10课 - 完成10课
7. ⭐ 25%进度 - 完成25%课程
8. ⭐ 50%进度 - 完成50%课程
9. ⭐ 75%进度 - 完成75%课程
10. 🏆 完成全部 - 完成全部50课
11. 📖 100例句 - 学习100个例句
12. 📖 500例句 - 学习500个例句
13. 📖 全部例句 - 学习全部例句
14. ✏️ 练习100题 - 完成100道练习题
15. ✏️ 练习500题 - 完成500道练习题
16. 🎯 正确率90% - 练习正确率达到90%
17. ⏰ 早起学习 - 上午8点前学习
18. 🌙 夜猫学习 - 晚上10点后学习
19. 💪 每日目标30天 - 连续30天完成每日目标
20. 🧠 全部精通 - 所有语法点达到Level 5

---

### 5.5 其他功能

#### 5.5.1 设置页面 (SettingsPage)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        设置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━ 学习设置 ━━━
每日目标:
• 目标例句数: [10] ▼
• 目标语法点数: [2] ▼

复习设置:
☑ 启用复习提醒
☑ 首页显示复习提醒

━━━ 音频设置 ━━━
☑ 自动播放音频
播放速度: [1.0x] ▼ (0.5x ~ 2.0x)

━━━ 显示设置 ━━━
主题: ○ 浅色 ● 深色 ○ 自动
字体大小: [中] ▼ (小/中/大)

━━━ 数据管理 ━━━
学习数据:
• 已学习例句: 40个
• 练习记录: 45条
• 数据占用: 2.3 MB

[导出学习数据 (JSON)]
[导入学习数据]
[重置所有数据] ⚠️

━━━ 关于 ━━━
版本: v1.0.0
数据来源: shin-kanzen N2 grammar
开源许可: CC BY-NC 4.0

[查看源代码] [反馈问题]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
1. 自定义每日目标
2. 复习提醒开关
3. 音频设置（自动播放、播放速度）
4. 主题切换（浅色/深色）
5. 字体大小调整
6. 数据导出/导入（JSON格式）
7. 数据重置（需二次确认）
8. 关于和版权信息

#### 5.5.2 首次使用引导 (OnboardingPage)

新用户第一次打开网站时显示:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   欢迎来到 JLPT N2 学习平台！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

        🎌

你将学习:
✓ 200+ N2核心语法点
✓ 1000+ 真实例句
✓ 完整日语发音

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[第1步: 学习方式介绍]

本平台采用结构化学习路径:
• 50个课程，每课3-5个语法点
• 必须完成前一课才能学习下一课
• 每个语法点配有详细例句和练习
• 科学的复习系统防止遗忘

[下一步]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[第2步: 学习建议]

建议学习计划:
• 每天学习 2-3 个语法点
• 每个语法点约 15 分钟
• 预计 3-4 个月完成全部内容

学习技巧:
✓ 重复听音频，培养语感
✓ 尝试造句，加深理解
✓ 按时复习，巩固记忆

[下一步]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[第3步: 设置每日目标]

你希望每天学习多少?

例句数: [10] ▼ (5/10/15/20)
语法点数: [2] ▼ (1/2/3/5)

稍后可以在设置中修改。

[完成，开始学习]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**功能**:
1. 3步引导流程
2. 介绍学习方式和建议
3. 设置每日目标
4. 只在首次使用时显示
5. 可跳过（右上角"跳过引导"按钮）

---

## 6. 开发实施计划

### 6.1 开发阶段划分

#### **Phase 1: MVP核心功能（Week 1-2）**

**目标**: 实现基本的学习流程，能够开始学习N2

**任务清单**:
1. ✅ 项目初始化
   - 创建Vite + React + TypeScript项目
   - 配置Tailwind CSS
   - 安装所有依赖

2. ✅ 数据层搭建
   - 配置Dexie.js (IndexedDB)
   - 定义数据库Schema
   - 实现CSV解析工具
   - 首次加载时导入数据

3. ✅ 核心页面
   - HomePage (简化版)
   - LessonListPage (课程地图)
   - LessonDetailPage (课程详情)
   - StudyPage (学习页面)

4. ✅ 核心组件
   - StudyCard (学习卡片)
   - AudioPlayer (音频播放)
   - ProgressBar (进度条)
   - LessonCard (课程卡片)

5. ✅ 状态管理
   - userStore (用户进度)
   - studyStore (学习状态)

6. ✅ 基础功能
   - 顺序解锁课程
   - 例句学习和标记
   - 语法点完成检测
   - 进度保存

**验收标准**:
- ✅ 可以加载CSV数据
- ✅ 可以浏览课程列表
- ✅ 可以学习例句并播放音频
- ✅ 进度正确保存到IndexedDB
- ✅ 完成一课后解锁下一课

---

#### **Phase 2: 练习与测试功能（Week 3）**

**目标**: 添加练习和测试，增强学习效果

**任务清单**:
1. ✅ 题目生成器
   - 实现填空题生成逻辑
   - 实现选择题生成逻辑
   - 干扰项生成算法

2. ✅ 课后小测
   - QuizPage (测试页面)
   - QuestionCard (题目卡片)
   - QuizResult (结果页面)
   - 完成语法点后触发小测

3. ✅ 课程测试
   - 课程完成80%后解锁
   - 测试结果统计
   - 通过后解锁下一课

4. ✅ 错题本
   - WrongAnswersPage (错题本页面)
   - 错题自动收集
   - 错题复习功能

5. ✅ 练习模式
   - PracticePage (练习模式选择)
   - 填空练习
   - 选择题练习
   - 范围筛选功能

**验收标准**:
- ✅ 完成语法点后自动触发小测
- ✅ 测试题目随机且合理
- ✅ 答题结果正确记录
- ✅ 错题正确加入错题本
- ✅ 可以自主选择练习模式

---

#### **Phase 3: 复习系统（Week 4）**

**目标**: 实现科学的复习系统，防止遗忘

**任务清单**:
1. ✅ 复习算法
   - 实现艾宾浩斯复习算法
   - 掌握等级管理
   - 下次复习日期计算

2. ✅ 复习提醒
   - 首页复习提醒组件
   - 到期复习项目查询
   - 推迟功能

3. ✅ 复习页面
   - ReviewPage (复习页面)
   - 复习模式选择（快速浏览/测试）
   - 复习完成统计
   - 自动更新复习计划

4. ✅ 复习记录
   - 记录复习历史
   - 更新掌握等级
   - 重新计算下次复习时间

**验收标准**:
- ✅ 学习后自动安排复习计划
- ✅ 首页正确显示到期复习项目
- ✅ 复习后正确更新掌握等级
- ✅ 复习间隔符合艾宾浩斯曲线

---

#### **Phase 4: 统计与激励（Week 5）**

**目标**: 完善统计功能，增加激励机制

**任务清单**:
1. ✅ 进度统计
   - ProgressPage (完整版)
   - 多维度统计数据
   - 本周学习图表
   - 掌握程度分布
   - 薄弱知识点分析

2. ✅ 每日目标
   - DailyGoal组件
   - 每日目标进度追踪
   - 完成提示

3. ✅ 学习时长
   - 学习时长统计
   - 平均每日学习时间
   - 本周学习时间

4. ✅ 连续学习
   - StudyStreak组件
   - 连续学习天数计算
   - 连续中断提醒

5. ✅ 成就系统
   - AchievementsPage (成就页面)
   - 成就列表定义
   - 成就解锁检测
   - 成就通知

**验收标准**:
- ✅ 统计数据准确完整
- ✅ 图表正确显示
- ✅ 每日目标正常工作
- ✅ 成就正确解锁和显示

---

#### **Phase 5: 优化与完善（Week 6）**

**目标**: 优化用户体验，修复Bug

**任务清单**:
1. ✅ 首次使用引导
   - OnboardingPage
   - 3步引导流程
   - 跳过功能

2. ✅ 设置页面
   - SettingsPage
   - 所有设置选项
   - 数据导出/导入
   - 数据重置

3. ✅ 性能优化
   - 懒加载音频
   - 虚拟滚动（课程列表）
   - IndexedDB查询优化
   - React性能优化

4. ✅ 响应式设计
   - 移动端适配
   - 平板端适配
   - 不同屏幕尺寸测试

5. ✅ PWA支持
   - manifest.json
   - Service Worker
   - 离线可用
   - 添加到主屏幕

6. ✅ Bug修复和测试
   - 功能测试
   - 兼容性测试
   - 性能测试

**验收标准**:
- ✅ 新用户有引导流程
- ✅ 所有设置功能正常
- ✅ 移动端体验良好
- ✅ 可以离线使用
- ✅ 无明显Bug

---

### 6.2 技术实现要点

#### 6.2.1 CSV数据加载流程

```typescript
// src/utils/csvParser.ts

import Papa from 'papaparse';
import { db } from '@/db/schema';

export async function loadCSVData(): Promise<void> {
  // 1. 检查IndexedDB是否已有数据
  const existingData = await db.sentences.count();
  if (existingData > 0) {
    console.log('Data already loaded');
    return;
  }

  // 2. 加载CSV文件
  const response = await fetch('/data/notes.csv');
  const csvText = await response.text();

  // 3. 解析CSV
  const { data } = Papa.parse(csvText, {
    header: true,
    skipEmptyLines: true,
    dynamicTyping: true,
  });

  // 4. 转换数据格式
  const sentences = data.map((row: any, index: number) => ({
    id: `sentence_${index + 1}`,
    lessonNumber: parseInt(row['课号']) || 0,
    grammarPoint: row['语法点'] || '',
    sentence: row['例句'] || '',
    furigana: row['假名标注'] || '',
    translation: row['中文翻译'] || '',
    audioPath: extractAudioPath(row['音频']),
    grammarConnection: row['语法接续'] || '',
    grammarExplanation: row['语法解释'] || '',
    wordByWord: row['逐词精解'] || '',
    tags: (row['标签'] || '').split(',').map((t: string) => t.trim()),
  }));

  // 5. 批量插入到IndexedDB
  await db.sentences.bulkAdd(sentences);

  // 6. 生成课程和语法点数据
  await generateLessonsAndGrammar(sentences);
  
  console.log('Data loaded successfully');
}

function extractAudioPath(audioField: string): string {
  // 处理 "[sound:example_002.mp3]" 格式
  const match = audioField?.match(/\[sound:(.+?)\]/);
  return match ? `/audio/${match[1]}` : '';
}
```

#### 6.2.2 IndexedDB配置 (Dexie)

```typescript
// src/db/schema.ts

import Dexie, { Table } from 'dexie';

export interface Sentence {
  id: string;
  lessonNumber: number;
  grammarPoint: string;
  sentence: string;
  furigana: string;
  translation: string;
  audioPath: string;
  grammarConnection: string;
  grammarExplanation: string;
  wordByWord: string;
  tags: string[];
}

export interface Lesson {
  id: number;
  grammarPoints: string[];
  sentenceCount: number;
  isUnlocked: boolean;
  isCompleted: boolean;
  completionRate: number;
}

export interface GrammarPoint {
  id: string;
  lessonNumber: number;
  sentenceIds: string[];
  sentenceCount: number;
  grammarConnection: string;
  grammarExplanation: string;
  isLearned: boolean;
}

export interface UserProgress {
  id: string;
  currentLessonId: number;
  currentGrammarPoint: string;
  learnedSentences: string[];
  learnedGrammar: Array<{
    grammarId: string;
    firstLearnedDate: Date;
    lastReviewedDate: Date;
    nextReviewDate: Date;
    reviewCount: number;
    masteryLevel: number;
  }>;
  completedLessons: number[];
  totalStudyTime: number;
  studyStreak: number;
  lastStudyDate: Date;
}

export interface DailyGoal {
  id: string;
  date: Date;
  targetSentences: number;
  targetGrammarPoints: number;
  completedSentences: number;
  completedGrammarPoints: number;
  studyTime: number;
  isCompleted: boolean;
}

export interface ExerciseRecord {
  id: string;
  sentenceId: string;
  grammarPoint: string;
  questionType: 'fill' | 'choice';
  userAnswer: string;
  correctAnswer: string;
  isCorrect: boolean;
  timestamp: Date;
}

export interface WrongAnswer {
  id: string;
  sentenceId: string;
  grammarPoint: string;
  wrongCount: number;
  lastWrongDate: Date;
  resolved: boolean;
  correctStreak: number;
}

export interface Achievement {
  id: string;
  name: string;
  description: string;
  icon: string;
  condition: string;
  isUnlocked: boolean;
  unlockedDate?: Date;
}

export class JLPTN2Database extends Dexie {
  sentences!: Table<Sentence, string>;
  lessons!: Table<Lesson, number>;
  grammarPoints!: Table<GrammarPoint, string>;
  userProgress!: Table<UserProgress, string>;
  dailyGoals!: Table<DailyGoal, string>;
  exerciseHistory!: Table<ExerciseRecord, string>;
  wrongAnswers!: Table<WrongAnswer, string>;
  achievements!: Table<Achievement, string>;

  constructor() {
    super('JLPTN2DB');
    
    this.version(1).stores({
      sentences: 'id, lessonNumber, grammarPoint',
      lessons: 'id',
      grammarPoints: 'id, lessonNumber',
      userProgress: 'id',
      dailyGoals: 'id, date',
      exerciseHistory: 'id, sentenceId, timestamp, isCorrect',
      wrongAnswers: 'id, grammarPoint, resolved',
      achievements: 'id, isUnlocked',
    });
  }
}

export const db = new JLPTN2Database();
```

#### 6.2.3 音频播放Hook

```typescript
// src/hooks/useAudio.ts

import { useState, useRef, useEffect } from 'react';

export function useAudio(audioPath: string) {
  const [isPlaying, setIsPlaying] = useState(false);
  const [duration, setDuration] = useState(0);
  const [currentTime, setCurrentTime] = useState(0);
  const audioRef = useRef<HTMLAudioElement | null>(null);

  useEffect(() => {
    // 预加载音频
    audioRef.current = new Audio(audioPath);
    
    audioRef.current.addEventListener('loadedmetadata', () => {
      setDuration(audioRef.current!.duration);
    });

    audioRef.current.addEventListener('timeupdate', () => {
      setCurrentTime(audioRef.current!.currentTime);
    });

    audioRef.current.addEventListener('ended', () => {
      setIsPlaying(false);
      setCurrentTime(0);
    });

    return () => {
      audioRef.current?.pause();
      audioRef.current = null;
    };
  }, [audioPath]);

  const play = () => {
    audioRef.current?.play();
    setIsPlaying(true);
  };

  const pause = () => {
    audioRef.current?.pause();
    setIsPlaying(false);
  };

  const toggle = () => {
    if (isPlaying) {
      pause();
    } else {
      play();
    }
  };

  const setPlaybackRate = (rate: number) => {
    if (audioRef.current) {
      audioRef.current.playbackRate = rate;
    }
  };

  return {
    isPlaying,
    duration,
    currentTime,
    play,
    pause,
    toggle,
    setPlaybackRate,
  };
}
```

#### 6.2.4 复习算法实现

```typescript
// src/utils/reviewAlgorithm.ts

export const REVIEW_INTERVALS = [1, 1, 3, 7, 15, 30]; // 天数

export interface ReviewSchedule {
  nextLevel: number;
  nextReviewDate: Date;
}

export function calculateNextReview(
  currentLevel: number,
  wasCorrect: boolean
): ReviewSchedule {
  // 答对：等级+1（最高5）
  // 答错：等级-1（最低1）
  let nextLevel = wasCorrect
    ? Math.min(currentLevel + 1, 5)
    : Math.max(currentLevel - 1, 1);

  // 根据新等级计算复习间隔
  const daysToAdd = REVIEW_INTERVALS[nextLevel];
  
  const nextReviewDate = new Date();
  nextReviewDate.setDate(nextReviewDate.getDate() + daysToAdd);
  nextReviewDate.setHours(0, 0, 0, 0); // 设置为当天00:00

  return { nextLevel, nextReviewDate };
}

export function getDueReviews(
  learnedGrammar: UserProgress['learnedGrammar']
): string[] {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  return learnedGrammar
    .filter(item => {
      const reviewDate = new Date(item.nextReviewDate);
      reviewDate.setHours(0, 0, 0, 0);
      return reviewDate <= today;
    })
    .map(item => item.grammarId);
}
```

#### 6.2.5 题目生成器

```typescript
// src/utils/quizGenerator.ts

import { db } from '@/db/schema';
import type { Sentence } from '@/db/schema';

export interface QuizQuestion {
  id: string;
  sentenceId: string;
  sentence: string;
  grammarPoint: string;
  translation: string;
  options: string[];
  correctAnswer: string;
  explanation: string;
}

export async function generateFillBlankQuestions(
  grammarPoint: string,
  count: number = 3
): Promise<QuizQuestion[]> {
  // 1. 获取该语法点的所有例句
  const sentences = await db.sentences
    .where('grammarPoint')
    .equals(grammarPoint)
    .toArray();

  if (sentences.length === 0) return [];

  // 2. 随机选择count个例句
  const selectedSentences = shuffleArray(sentences).slice(0, count);

  // 3. 获取相似的语法点作为干扰项
  const distractors = await getSimilarGrammarPoints(grammarPoint, 3);

  // 4. 生成题目
  return selectedSentences.map(sentence => ({
    id: `quiz_${sentence.id}_${Date.now()}`,
    sentenceId: sentence.id,
    sentence: sentence.sentence.replace(grammarPoint, '______'),
    grammarPoint: grammarPoint,
    translation: sentence.translation,
    options: shuffleArray([grammarPoint, ...distractors]),
    correctAnswer: grammarPoint,
    explanation: sentence.grammarExplanation,
  }));
}

async function getSimilarGrammarPoints(
  targetGrammar: string,
  count: number
): Promise<string[]> {
  // 获取同课或相近课程的其他语法点作为干扰项
  const target = await db.grammarPoints.get(targetGrammar);
  if (!target) return [];

  const similar = await db.grammarPoints
    .where('lessonNumber')
    .between(target.lessonNumber - 2, target.lessonNumber + 2)
    .and(g => g.id !== targetGrammar)
    .limit(count * 3)
    .toArray();

  return shuffleArray(similar.map(g => g.id)).slice(0, count);
}

function shuffleArray<T>(array: T[]): T[] {
  const shuffled = [...array];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}
```

---

## 7. UI/UX设计规范

### 7.1 颜色系统

```css
/* 主色调 */
--primary: #3B82F6;      /* 蓝色 - 主要按钮、链接 */
--primary-hover: #2563EB;
--primary-light: #DBEAFE;

/* 成功/进度 */
--success: #10B981;      /* 绿色 - 完成状态 */
--success-light: #D1FAE5;

/* 警告 */
--warning: #F59E0B;      /* 橙色 - 警告提示 */
--warning-light: #FEF3C7;

/* 错误 */
--error: #EF4444;        /* 红色 - 错误、答错 */
--error-light: #FEE2E2;

/* 中性色 */
--gray-50: #F9FAFB;
--gray-100: #F3F4F6;
--gray-200: #E5E7EB;
--gray-300: #D1D5DB;
--gray-400: #9CA3AF;
--gray-500: #6B7280;
--gray-600: #4B5563;
--gray-700: #374151;
--gray-800: #1F2937;
--gray-900: #111827;

/* 语法点高亮 */
--highlight: #FEF08A;    /* 黄色背景 */
--highlight-border: #EAB308;

/* 锁定状态 */
--locked: #9CA3AF;       /* 灰色 */
```

### 7.2 字体设置

```css
/* 日语字体 */
font-family: 'Noto Sans JP', 'Hiragino Sans', 'Hiragino Kaku Gothic ProN', 
             'Yu Gothic', 'Meiryo', sans-serif;

/* 字号 */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */

/* 行高 */
--leading-normal: 1.5;
--leading-relaxed: 1.75;
```

### 7.3 组件样式

#### 按钮样式
```tsx
// Primary Button
className="bg-primary hover:bg-primary-hover text-white 
           px-6 py-3 rounded-lg font-medium 
           transition-colors duration-200
           shadow-md hover:shadow-lg"

// Secondary Button
className="bg-gray-200 hover:bg-gray-300 text-gray-800
           px-6 py-3 rounded-lg font-medium
           transition-colors duration-200"

// Disabled Button
className="bg-gray-300 text-gray-500 cursor-not-allowed
           px-6 py-3 rounded-lg font-medium"
```

#### 卡片样式
```tsx
className="bg-white rounded-xl shadow-md p-6
           border border-gray-200
           hover:shadow-lg transition-shadow duration-200"
```

#### 进度条样式
```tsx
// 外层容器
className="w-full bg-gray-200 rounded-full h-4"

// 内层进度
className="bg-primary h-full rounded-full transition-all duration-300"
style={{ width: `${progress}%` }}
```

### 7.4 响应式断点

```css
/* 移动端 */
@media (max-width: 640px) { /* sm */ }

/* 平板 */
@media (min-width: 768px) { /* md */ }

/* 桌面 */
@media (min-width: 1024px) { /* lg */ }
@media (min-width: 1280px) { /* xl */ }
```

---

## 8. 测试计划

### 8.1 功能测试清单

#### 数据加载测试
- [ ] CSV正确解析
- [ ] 数据正确存入IndexedDB
- [ ] 课程和语法点正确生成
- [ ] 音频文件路径正确

#### 学习流程测试
- [ ] 课程正确解锁
- [ ] 例句正确显示
- [ ] 音频正常播放
- [ ] 进度正确保存
- [ ] 语法点完成检测正确

#### 练习测试
- [ ] 题目正确生成
- [ ] 答案判断准确
- [ ] 错题正确记录
- [ ] 测试通过逻辑正确

#### 复习测试
- [ ] 复习日期计算正确
- [ ] 到期项目正确显示
- [ ] 复习后等级更新正确
- [ ] 下次复习时间正确

#### 统计测试
- [ ] 学习时长统计准确
- [ ] 完成度计算正确
- [ ] 图表数据准确
- [ ] 成就解锁正确

### 8.2 兼容性测试

**浏览器**:
- [ ] Chrome (最新版)
- [ ] Firefox (最新版)
- [ ] Safari (最新版)
- [ ] Edge (最新版)

**设备**:
- [ ] iPhone (iOS Safari)
- [ ] Android (Chrome)
- [ ] iPad
- [ ] 桌面浏览器

### 8.3 性能测试

- [ ] 首屏加载时间 < 2秒
- [ ] CSV解析时间 < 1秒
- [ ] 页面切换流畅（无卡顿）
- [ ] 音频播放延迟 < 200ms
- [ ] IndexedDB查询响应 < 100ms

---

## 9. 部署指南

### 9.1 构建命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 9.2 部署到Vercel

```bash
# 安装Vercel CLI
npm i -g vercel

# 登录
vercel login

# 部署
vercel --prod
```

### 9.3 部署到Netlify

```bash
# 安装Netlify CLI
npm i -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod --dir=dist
```

### 9.4 环境变量

```env
# .env.example
VITE_APP_TITLE=JLPT N2 学习平台
VITE_DATA_PATH=/data/notes.csv
VITE_AUDIO_PATH=/audio
```

---

## 10. 版权和许可

### 10.1 数据来源

数据来自开源项目: 
https://github.com/mxggle/anki-jlpt-n2-grammar-example-sentences

### 10.2 许可证

CC BY-NC 4.0 (知识共享 署名-非商业性使用 4.0)

**要求**:
- ✅ 必须署名原作者
- ✅ 必须注明许可协议
- ❌ 不得用于商业用途
- ✅ 允许修改和再分发

### 10.3 网站底部声明

```
本网站使用的学习数据来自 shin-kanzen N2 grammar 项目
数据许可: CC BY-NC 4.0
本网站仅供个人学习使用，严禁商业用途
```

---

## 11. 未来扩展方向

### 11.1 短期优化（1-2个月）
- [ ] 听力练习模式增强
- [ ] 更多练习题型（翻译、造句）
- [ ] 学习笔记功能
- [ ] 收藏例句导出

### 11.2 中期规划（3-6个月）
- [ ] 添加N1、N3级别
- [ ] 用户账号系统（云端同步）
- [ ] PostgreSQL后端
- [ ] 社区功能（讨论区）

### 11.3 长期愿景（6个月+）
- [ ] AI对话练习（结合Claude API）
- [ ] 个性化学习路径推荐
- [ ] 移动App（React Native）
- [ ] 学习伙伴匹配

---

## 12. 附录

### 12.1 依赖包列表

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "zustand": "^4.4.7",
    "dexie": "^3.2.4",
    "dexie-react-hooks": "^1.1.7",
    "papaparse": "^5.4.1",
    "lucide-react": "^0.294.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@types/papaparse": "^5.3.12",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.0",
    "tailwindcss": "^3.3.6",
    "postcss": "^8.4.32",
    "autoprefixer": "^10.4.16"
  }
}
```

### 12.2 文件夹容量预估

```
项目总大小: ~100-150MB

分布:
- 源代码: ~5MB
- node_modules: ~200MB (开发时)
- 构建产物: ~2MB
- CSV数据: ~1MB
- 音频文件: ~50-100MB (取决于音频质量)
```

### 12.3 快速开始检查清单

#### 开发前准备
- [ ] Node.js 18+ 已安装
- [ ] Git 已安装
- [ ] VS Code (或其他编辑器)
- [ ] 从源项目下载 notes.csv 和音频文件

#### Claude Code 开发指令

```
请按照以下PRD开发JLPT N2学习网站:
[粘贴此PRD]

开发优先级:
1. Phase 1: MVP核心功能（Week 1-2）
2. Phase 2: 练习与测试功能（Week 3）
3. Phase 3: 复习系统（Week 4）
4. Phase 4: 统计与激励（Week 5）
5. Phase 5: 优化与完善（Week 6）

请从Phase 1开始，完成后再进入下一个阶段。
```

---

## 13. 成功指标

### 13.1 开发成功指标
- ✅ 所有Phase按时完成
- ✅ 核心功能100%实现
- ✅ 无阻塞性Bug
- ✅ 性能指标达标

### 13.2 学习效果指标
- ✅ 用户能够按课程顺序完成学习
- ✅ 课后测试平均通过率 > 60%
- ✅ 用户连续学习 > 7天
- ✅ 复习系统有效使用

### 13.3 用户体验指标
- ✅ 新用户5分钟内理解使用方式
- ✅ 移动端和桌面端体验一致
- ✅ 无明显卡顿和延迟
- ✅ 进度保存可靠（不丢失）

---

## 📝 总结

这是一个**零成本、纯前端**的JLPT N2学习网站PRD，完全基于IndexedDB，无需后端服务器。

**核心特点**:
✅ 结构化学习路径（50课顺序解锁）
✅ 科学复习系统（艾宾浩斯曲线）
✅ 完整练习体系（课后测试+错题本）
✅ 激励机制（成就、连续学习）
✅ 完全离线可用（PWA）

**开发周期**: 4-6周
**技术栈**: React + TypeScript + IndexedDB
**部署**: Vercel/Netlify（免费）

按照本PRD开发，可以快速上线一个功能完整、体验优秀的N2学习平台，帮助你系统化地完成N2学习！

祝学习顺利！📚🎌